let notas: integer[] = [90, 85, 100];
let total: integer = 0;

foreach (n in notas) {
  total = total + n;
}

let matriz: integer[][] = [[1, 2], [3, 4]];
foreach (fila in matriz) {
  print(fila[0]);
}

print(total);
